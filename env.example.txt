# ==================== HR 자동화 시스템 v4.0 (Windows Docker 홈서버) ====================
# 
# 사용법:
# 1. 이 파일을 .env로 복사: copy env.example.txt .env
# 2. 아래 값들을 실제 환경에 맞게 수정
# 3. docker-compose up -d 실행
#
# ==================================================================================

# ==================== 데이터베이스 ====================
DB_PATH=./hr_master.db

# ==================== 회사 정보 ====================
COMPANY_NAME=우리회사
COMPANY_CEO=대표자명
BUSINESS_NUMBER=123-45-67890

# ==================== 보안 (⚠️ 반드시 변경하세요!) ====================
SECRET_KEY=your_secret_key_here_change_this_for_production_use
ADMIN_PASSWORD=admin1234

# ==================== 네트워크 ====================
# Windows 홈서버 내부 IP 주소
# 확인 방법: PowerShell에서 'ipconfig' 실행 → IPv4 주소 확인
# 예: 192.168.0.100, 192.168.1.50, 10.0.0.5 등
HOST_IP=192.168.0.100

# 외부 도메인 (선택사항)
# 공유기 포트포워딩 설정 시 사용
# 예: mycompany.example.com
EXTERNAL_DOMAIN=

# ==================== 포트 설정 ====================
# v4.0에서는 통합 대시보드(8000)만 사용
DASHBOARD_PORT=8000

# Nginx 웹서버 포트 (80 또는 8080)
# 80번 포트는 관리자 권한 필요 (Windows에서는 보통 가능)
NGINX_PORT=80

# ==================== 로그 설정 ====================
LOG_LEVEL=INFO
LOG_RETENTION_DAYS=30

# ==================== Streamlit 설정 ====================
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
STREAMLIT_SERVER_FILE_WATCHER_TYPE=none
STREAMLIT_SERVER_MAX_UPLOAD_SIZE=200

# ==================== Docker 설정 ====================
COMPOSE_PROJECT_NAME=hr-automation
RESTART_POLICY=always

# ==================== 백업 설정 (선택사항) ====================
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *  # 매일 새벽 2시
BACKUP_RETENTION_DAYS=7